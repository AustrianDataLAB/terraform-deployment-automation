coder.yml
v2:
  metadata:
    title: "Openstack via CODER"
  job:
    adapter: "coder"
    host: "" # add host url
    cluster: "coder"
    token: "" # add token for coder
  batch_connect:
    ssh_allow: false
